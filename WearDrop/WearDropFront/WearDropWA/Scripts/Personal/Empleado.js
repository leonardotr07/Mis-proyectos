let modalFormError;
function mostrarModalError() {
    //Le indicas que modal vas a usar en el getElementById
    modalFormError = new bootstrap.
        Modal(document.getElementById('errorModal'));
    modalFormError.show();
}